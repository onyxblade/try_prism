# 巢狀設定:用 group 把相關設定分組成子 Hash。
set :host, "localhost"
set :port, 3000

group :database do
  set :adapter, "postgres"
  set :pool, 5

  group :replica do
    set :host, "replica.local"
    set :port, 5433
  end
end

group :logging do
  set :level, "info"
  set :file, "/var/log/app.log"
end

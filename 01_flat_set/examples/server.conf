# 最基礎的 DSL 用例:寫一份 server 設定檔。
#
# 它「長得像 Ruby」——這正是 method based DSL 的賣點:
# 使用者不必學新語法,編輯器也能高亮。
# 但在我們的研究裡,目標是「解析(parse)」這份檔案,而不是「執行(eval)」它。

set :host, "localhost"
set :port, 3000
set :workers, 4
set :tls, true

# 看起來只比 server.conf 多一行,但那一行是「任意程式碼」。
# 在 method based(instance_eval)版本裡,這行 system 會真的被執行。
# 在 Prism 版裡,它只是一個我們不認得的 CallNode,會被拒絕。

set :host, "localhost"
system("echo '>>> 任意指令被執行了 <<<'")
set :port, 3000

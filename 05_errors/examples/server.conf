# 一份用到目前為止全部特性(set/group/陣列/雜湊/ref)的合法設定。
set :host, "db.internal"
set :port, 5432
set :primary, ref(:host)

group :replica do
  set :host, ref(:host)
  set :pair, [ref(:host), ref(:port)]
end

group :pool do
  set :endpoints, { primary: ref(:primary), port: ref(:port) }
end

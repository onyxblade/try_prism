# 故意在不同行塞進各種違規。以前的版本會在第一個就停;
# 現在一次回報全部。注意:即使有 system / File.read,也沒有任何一行被執行。
set :host, "localhost"
set :port, "3000", "extra"
frobnicate :x
set "name", "app"
set :tags, [1, system("rm -rf /")]
group :db
set :pw, File.read("/etc/passwd")
